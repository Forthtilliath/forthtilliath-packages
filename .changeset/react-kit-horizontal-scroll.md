---
"@forthtilliath/react-kit": minor
---

Add `useHorizontalScroll({ step?, keyboard? })`: tracks whether a horizontally scrollable container can scroll left/right (kept in sync by a `ResizeObserver`), with `scrollByStep` and optional arrow-key scrolling outside form fields.
