---
"@forthtilliath/ts-kit": minor
---

Add `swapItems(items, i, j)` to the `array` category: returns a copy of `items` with the elements at `i` and `j` swapped, or a plain copy if either position is out of bounds.
