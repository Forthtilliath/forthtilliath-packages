---
"@forthtilliath/ts-kit": minor
---

Add `sortUpcomingFirst(items, getDates, { now? })` to the `array` category: upcoming items first (nearest first), then past ones (most recent first), undated last. Items may carry several dates.
